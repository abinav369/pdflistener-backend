# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Auth & security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Database
sqlalchemy==2.0.36

# Settings
pydantic-settings>=2.7.0
pydantic[email]>=2.10.0

# File uploads
python-multipart==0.0.12

# PDF text extraction
PyMuPDF==1.24.13

# DOCX text extraction
python-docx==1.1.2

# Text-to-speech (free tier)
gTTS==2.5.3

# Audio duration reading
mutagen==1.47.0

# Audio concatenation for large docs (optional, needed if doc > 4500 chars with gTTS)
# pydub==0.25.1
# Install ffmpeg separately: sudo apt install ffmpeg

# OpenAI TTS (optional, high quality)
# openai==1.55.0
